language: node_js
node_js:
  - lts/*
install:
  - pnpm install # npm ci
script:
  - pnpm docs:build # npm run docs:build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: docs
  # A token generated on GitHub allowing Travis to push code on you repository.
  # Set in the Travis settings page of your repository, as a secure variable.
  github_token: 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
  keep_history: true
  on:
    branch: main
